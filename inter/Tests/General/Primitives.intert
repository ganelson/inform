package main _plain
	kind K_unchecked unchecked
	kind K_unchecked_function function K_unchecked -> K_unchecked
	kind K_unchecked_list list of K_unchecked
	kind K_number int32
	kind K_int2 int2
	kind K_phrase_number____nothing function K_number -> void
	package MyRoutine_B _code
		local (/main/K_number) x
		local (/main/K_number) y
		code
			inv !return
				val (/main/K_number) x
			inv !return
				inv !plus
					val (/main/K_number) x
					val (/main/K_number) y
			inv !return
				inv !minus
					val (/main/K_number) x
					val (/main/K_number) y
			inv !return
				inv !modulo
					val (/main/K_number) x
					val (/main/K_number) y
	constant (K_phrase_number____nothing) MyRoutine = MyRoutine_B
